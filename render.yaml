services:
  - type: web
    name: springboot-app
    runtime: java
    buildCommand: "mvn clean package"
    startCommand: "java -jar target/*.jar"
    envVars:
      - key: JAVA_VERSION
        value: 17
